"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.guard = exports.pick = exports.concatBytes = exports.arraysEqual = exports.bytesEqual = void 0;
/** TODO: docs */
function bytesEqual(a, b) {
    return arraysEqual(a, b);
}
exports.bytesEqual = bytesEqual;
/** TODO: docs */
function arraysEqual(a, b) {
    if (a === b)
        return true;
    const length = a.length;
    if (length !== b.length)
        return false;
    for (let i = 0; i < length; i++) {
        if (a[i] !== b[i])
            return false;
    }
    return true;
}
exports.arraysEqual = arraysEqual;
/** TODO: docs */
function concatBytes(first, ...others) {
    const length = others.reduce((length, bytes) => length + bytes.length, first.length);
    const bytes = new Uint8Array(length);
    bytes.set(first, 0);
    for (const other of others) {
        bytes.set(other, bytes.length);
    }
    return bytes;
}
exports.concatBytes = concatBytes;
/** TODO: docs */
function pick(object, ...keys) {
    const picked = {};
    for (const key of keys) {
        picked[key] = object[key];
    }
    return picked;
}
exports.pick = pick;
/** TODO: docs */
function guard(callback) {
    try {
        callback();
    }
    catch (error) {
        console.error(error);
    }
}
exports.guard = guard;
//# sourceMappingURL=util.js.map